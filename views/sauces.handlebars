<section>
    <h2>There are {{sauces.length}} sauces</h2>
    {{#each sauces}} {{!-- for each sauces in sauces, create the following html --}}
    <article class = "clearfix item-container block">
        <h2>{{name}}</h2>
        <img class="item-img" src="{{image}}" />
    </article>
    {{/each}}
</section>